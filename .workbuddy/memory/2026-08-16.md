# 2026-08-16 工作日志

## 项目进度盘点（knowflow-ai）
- 当前分支：`feature/rbac`，工作区干净（Git 已提交，无未提交改动）。
- 新增提交（自 8-15 起 3 个）：
  - `38d4ed0` Phase 3 企业模块完成（新增部门管理：`enterprise_department` 表 + DepartmentService/Controller，父子层级 + 树形 VO）
  - `da20561` 成员可退出企业
  - `8d04ec4` Phase 4 RBAC 权限系统（+1687 行，34 文件）
- RBAC 核心变化：`enterprise_member.member_role` 字符串列退役（V7 删除），角色改为 `enterprise_role`（OWNER/ADMIN/MEMBER 编码）+ `permission`（如 enterprise:update）+ `enterprise_role_permission` 关联表（V6 创建）；采用两阶段迁移（V6 加列回填 → V7 删旧列）。
- 新增 `EnterpriseMembershipChecker`：统一收口「企业存在 + 当前用户为正常成员」校验（原来散落在企业/邀请/部门三个 Service）。
- Controller 侧用 `@PreAuthorize` 按权限码做管理操作校验，与 Service 成员校验构成纵深防御。
- 邀请流程仍以字符串角色（member_role）发起，接受时映射为目标企业角色 ID。
- 迁移脚本现为 V1~V7（sys_user / enterprise / enterprise_member / enterprise_invitation / enterprise_department / rbac 三表 / drop member_role）。
- `docs/` 新增「AI 企业知识库 - 智能客服 SaaS 项目长期教学提示词.md」（AI 教学角色设定，确认项目为教学导向）。
- 技术栈不变：Spring Boot 4.1 + Java 21 + MyBatis-Plus + Security + Redis + Flyway + MySQL/H2。
- 注意：本机 Git Bash 下 `mvn` 不可用（路径问题），须用 PowerShell/cmd 执行 `mvn.cmd`，并临时设置 `JAVA_HOME=D:\software\jdk-21.0.8`。
